<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the visual theme; this only styles the splash window. -->
    <style name="Theme.DacsAttendance" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
